
@model IEnumerable<elearning_b1.Models.ReadingSkill>

@{
    ViewData["Title"] = "Danh sách bài đọc";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container mt-4">
    <form method="get" class="mb-4">
        <div class="input-group">
            <input type="text" name="searchQuery" class="form-control" placeholder="Tìm kiếm bài đọc..." value="@ViewContext.HttpContext.Request.Query["searchQuery"]"/>
            <button type="submit" class="btn btn-primary">Tìm</button>
        </div>
    </form>
    <!-- Danh sách bài đọc -->
    <div class="row">
        <div class="container mt-4">
            <h2 class="text-center mb-4">BẮT ĐẦU TỪ NHỮNG DÒNG CHỮ, TIẾNG ANH THẬT GẦN GŨI!</h2>
            <p class ="text-center mb-4">Mỗi đoạn văn, mỗi từ vựng đều mang trong mình một câu chuyện. Hãy lắng nghe chúng và cảm nhận sự tiến bộ của chính bạn!</p>
            <div class="row">
                @foreach (var item in Model)
                {
                    <div class="col-md-4 mb-3">
                        <div class="card shadow-sm">
                            <div class="card-body">
                                <h5 class="card-title">@item.Title</h5>
                                <p class="card-text text-truncate" style="max-height: 60px; overflow: hidden;">
                                    @Html.Raw(item.Content)
                                </p>
                                <a href="@Url.Action("Exercise", "Reading", new { id = item.Id })" class="btn btn-primary btn-sm">Xem chi tiết</a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<style>
    .card-title {
        color: #007bff;
        font-weight: bold;
    }

    .card:hover {
        transform: scale(1.05);
        transition: 0.3s ease-in-out;
    }

    .text-truncate {
        white-space: nowrap;
        text-overflow: ellipsis;
        overflow: hidden;
    }

</style>
